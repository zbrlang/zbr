name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.asset_name }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            binary: zbr
            asset_name: zbr-linux-x64
            cross: false
          - os: ubuntu-latest
            target: aarch64-linux-android
            binary: zbr
            asset_name: zbr-linux-arm64
            cross: true
          - os: macos-latest
            target: x86_64-apple-darwin
            binary: zbr
            asset_name: zbr-darwin-x64
            cross: false
          - os: macos-latest
            target: aarch64-apple-darwin
            binary: zbr
            asset_name: zbr-darwin-arm64
            cross: false
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            binary: zbr.exe
            asset_name: zbr-windows-x64.exe
            cross: false
          - os: windows-latest
            target: aarch64-pc-windows-msvc
            binary: zbr.exe
            asset_name: zbr-windows-arm64.exe
            cross: false

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ matrix.os }}-${{ matrix.target }}-cargo-${{ hashFiles('**/Cargo.lock') }}

      - name: Install cross
        if: matrix.cross == true
        run: cargo install cross

      - name: Build
        shell: bash
        run: |
          if [ "${{ matrix.cross }}" == "true" ]; then
            cross build --release --target ${{ matrix.target }}
          else
            cargo build --release --target ${{ matrix.target }}
          fi

      - name: Rename binary
        shell: bash
        run: |
          if [ "${{ matrix.binary }}" != "${{ matrix.asset_name }}" ]; then
            mv target/${{ matrix.target }}/release/${{ matrix.binary }} target/${{ matrix.target }}/release/${{ matrix.asset_name }}
          fi

      - name: Create Release and Upload Asset
        uses: softprops/action-gh-release@v1
        with:
          files: target/${{ matrix.target }}/release/${{ matrix.asset_name }}
          token: ${{ secrets.GITHUB_TOKEN }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  deploy_all:
    name: Deploy to All Channels
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.DISTRIBUTION_SSH_KEY }}

      - name: Debug SSH Keys
        run: ssh-add -l

      - name: Extract version from tag
        id: tag_version
        run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT

      - name: Download Binaries
        run: |
          gh release download ${{ github.ref_name }} --pattern "zbr-*"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # --- NPM (Temporarily Disabled - Uncomment to re-enable) ---
      # - name: Setup Node
      #   uses: actions/setup-node@v4
      #   with:
      #     node-version: '20'
      #     registry-url: 'https://registry.npmjs.org'
      # - name: Publish to NPM
      #   run: |
      #     # (NPM logic here)
      #   env:
      #     NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Calculate Hashes
        id: hashes
        run: |
          echo "DARWIN_X64=$(sha256sum zbr-darwin-x64 | cut -d' ' -f1)" >> $GITHUB_ENV
          echo "DARWIN_ARM64=$(sha256sum zbr-darwin-arm64 | cut -d' ' -f1)" >> $GITHUB_ENV
          echo "LINUX_X64=$(sha256sum zbr-linux-x64 | cut -d' ' -f1)" >> $GITHUB_ENV
          echo "LINUX_ARM64=$(sha256sum zbr-linux-arm64 | cut -d' ' -f1)" >> $GITHUB_ENV
          echo "WIN_X64=$(sha256sum zbr-windows-x64.exe | cut -d' ' -f1)" >> $GITHUB_ENV
          echo "WIN_ARM64=$(sha256sum zbr-windows-arm64.exe | cut -d' ' -f1)" >> $GITHUB_ENV

      - name: Update Homebrew
        run: |
          if [ ! -f homebrew/zbr.rb ]; then echo "Error: homebrew/zbr.rb missing"; exit 1; fi
          rm -rf clone_homebrew
          GIT_SSH_COMMAND="ssh -o IdentitiesOnly=yes" git clone git@github.com:zbrlang/homebrew-tap.git clone_homebrew
          mkdir -p clone_homebrew/Formula
          cp homebrew/zbr.rb clone_homebrew/Formula/zbr.rb
          sed -i "s/REPLACE_ME_SHA256_DARWIN_X64/$DARWIN_X64/" clone_homebrew/Formula/zbr.rb
          sed -i "s/REPLACE_ME_SHA256_DARWIN_ARM64/$DARWIN_ARM64/" clone_homebrew/Formula/zbr.rb
          sed -i "s/REPLACE_ME_SHA256_LINUX_X64/$LINUX_X64/" clone_homebrew/Formula/zbr.rb
          sed -i "s/REPLACE_ME_SHA256_LINUX_ARM64/$LINUX_ARM64/" clone_homebrew/Formula/zbr.rb
          cd clone_homebrew
          git config user.name "ZBR CI"
          git config user.email "ci@zbrlang.com"
          git add .
          if ! git diff-index --quiet HEAD; then GIT_SSH_COMMAND="ssh -o IdentitiesOnly=yes" git commit -m "Update zbr to ${{ github.ref_name }}"; GIT_SSH_COMMAND="ssh -o IdentitiesOnly=yes" git push; else echo "No changes to commit."; fi

      - name: Update AUR
        run: |
          if [ ! -f aur/PKGBUILD ]; then echo "Error: aur/PKGBUILD missing"; exit 1; fi
          rm -rf clone_aur
          GIT_SSH_COMMAND="ssh -o IdentitiesOnly=yes" git clone git@github.com:zbrlang/aur-package.git clone_aur
          cp aur/PKGBUILD clone_aur/PKGBUILD
          sed -i "s/pkgver=.*$/pkgver=${{ steps.tag_version.outputs.VERSION }}/" clone_aur/PKGBUILD
          sed -i "s/REPLACE_ME_SHA256_LINUX_X64/$LINUX_X64/" clone_aur/PKGBUILD
          sed -i "s/REPLACE_ME_SHA256_LINUX_ARM64/$LINUX_ARM64/" clone_aur/PKGBUILD
          cd clone_aur
          git config user.name "ZBR CI"
          git config user.email "ci@zbrlang.com"
          git add .
          if ! git diff-index --quiet HEAD; then GIT_SSH_COMMAND="ssh -o IdentitiesOnly=yes" git commit -m "Update zbr to ${{ github.ref_name }}"; GIT_SSH_COMMAND="ssh -o IdentitiesOnly=yes" git push; else echo "No changes to commit."; fi

      - name: Update Winget
        run: |
          if [ ! -f winget/zbr.yaml ]; then echo "Error: winget/zbr.yaml missing"; exit 1; fi
          rm -rf clone_winget
          GIT_SSH_COMMAND="ssh -o IdentitiesOnly=yes" git clone git@github.com:zbrlang/winget-packages.git clone_winget
          cp winget/zbr.yaml clone_winget/zbr.yaml
          sed -i "s/PackageVersion: .*$/PackageVersion: ${{ steps.tag_version.outputs.VERSION }}/" clone_winget/zbr.yaml
          sed -i "s/REPLACE_ME_SHA256_WINDOWS_X64/$WIN_X64/" clone_winget/zbr.yaml
          sed -i "s/REPLACE_ME_SHA256_WINDOWS_ARM64/$WIN_ARM64/" clone_winget/zbr.yaml
          cd clone_winget
          git config user.name "ZBR CI"
          git config user.email "ci@zbrlang.com"
          git add .
          if ! git diff-index --quiet HEAD; then GIT_SSH_COMMAND="ssh -o IdentitiesOnly=yes" git commit -m "Update zbr to ${{ github.ref_name }}"; GIT_SSH_COMMAND="ssh -o IdentitiesOnly=yes" git push; else echo "No changes to commit."; fi

      - name: Update Scoop
        run: |
          if [ ! -f scoop/zbr.json ]; then echo "Error: scoop/zbr.json missing"; exit 1; fi
          rm -rf clone_scoop
          GIT_SSH_COMMAND="ssh -o IdentitiesOnly=yes" git clone git@github.com:zbrlang/scoop-bucket.git clone_scoop
          cp scoop/zbr.json clone_scoop/zbr.json
          sed -i "s/REPLACE_ME_SHA256_WINDOWS_X64/$WIN_X64/" clone_scoop/zbr.json
          sed -i "s/REPLACE_ME_SHA256_WINDOWS_ARM64/$WIN_ARM64/" clone_scoop/zbr.json
          cd clone_scoop
          git config user.name "ZBR CI"
          git config user.email "ci@zbrlang.com"
          git add .
          if ! git diff-index --quiet HEAD; then GIT_SSH_COMMAND="ssh -o IdentitiesOnly=yes" git commit -m "Update zbr to ${{ github.ref_name }}"; GIT_SSH_COMMAND="ssh -o IdentitiesOnly=yes" git push; else echo "No changes to commit."; fi

  rebuild_hf_space:
    name: Rebuild HF Space
    needs: deploy_all
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Rebuild
        run: |
          git clone https://Zubariel:${{ secrets.HF_TOKEN }}@huggingface.co/spaces/zbrlang/zbr-server hf-space
          cd hf-space
          echo ${{ github.ref_name }} > .build-version
          sed -i "s/ARG CACHEBUST=.*/ARG CACHEBUST=$(date +%s)/" Dockerfile
          git config user.email "ci@zbrlang.com"
          git config user.name "ZBR CI"
          git add .build-version Dockerfile
          git commit -m "rebuild for ${{ github.ref_name }}"
          git push
